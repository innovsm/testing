services:
  - type: web
    name: mcp-demo
    env: python
    plan: free
    region: oregon  # Optional: choose a region close to you
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    autoDeploy: true  # Set to false if you prefer manual deploys
    envVars:
      - key: PORT
        value: 8000  # Render overrides this with its own port, but setting it ensures local consistency
    healthCheckPath: /health